# PAUL Handoff

**Date:** 2026-05-06
**Status:** paused — plan approved, ready for APPLY

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** nextflow — self-hosted personal productivity app
**Core value:** Users can track tasks, projects, and calendar events across any browser on their network — self-hosted, zero cloud dependency, real-time sync.

---

## Current State

**Version:** v1.0 Feedback Clearance & Polish
**Phase:** 2 of 6 — Inbox & Clarify
**Plan:** 02-01 — created, awaiting APPLY

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○
```

---

## What Was Done This Session

- Closed Phase 1 loop (all 4 bugs from prior session already committed)
- Ran /paul:discuss 2 to explore Phase 2 goals with user
- Clarified exact issues for each of the 4 feedback items:
  - `0bf1bf88` — "New project" in clarify doesn't pre-fill name from task title, and after routing gives no chance to set project details; fix = pre-fill + auto-open project flyout
  - `160e0923` — `clarifyDescSummary` element exists in HTML and listener is wired, but it's permanently `hidden`; fix = un-hide + populate in `populateClarifyPreview`
  - `bb6a0dba` — Already implemented (My Day radio already in HTML). Marked resolved via PATCH /feedback API.
  - `c4c05706` — Already implemented (textarea + handler wired). Marked resolved via PATCH /feedback API.
- Created `.paul/phases/02-inbox-clarify/CONTEXT.md`
- Created `.paul/phases/02-inbox-clarify/02-01-PLAN.md` (3 tasks, autonomous, standard scope)
- Updated STATE.md and ROADMAP.md (Phase 2 → Planning)

---

## What's In Progress

**02-01-PLAN.md** — created and ready, not yet executed

---

## What's Next

**Immediate:** Run `/paul:apply .paul/phases/02-inbox-clarify/02-01-PLAN.md`

The plan has 3 tasks, all in `app/web_ui/js/ui.js`:

1. **Task 1** — In `handleClarifyConvertToProject()` (~line 6165): pre-fill the name input with `clarifyState.previewText`. In `finalizeNewProject()` closure (~line 4997): add `this.clarifyState.convertedProjectId = project.id`. In `resetClarifyState()`: add `convertedProjectId: null` to state object.

2. **Task 2** — In `finalizeClarifyRouting()` (~line 6387): capture `convertedProjectId` and `inSession` BEFORE calling `_completeClarifyStep` (state resets during close). After `_completeClarifyStep`, call `this.openProjectFlyout(convertedProjectId)` if `convertedProjectId && !inSession`.

3. **Task 3** — In `populateClarifyPreview(task)` (~line 5810): un-hide `clarifyDescSummary` and populate with `task.description`. In `resetClarifyState()`: re-hide and clear it.

**After that:** Run `/paul:unify` to close the loop.

---

## Key Decisions Captured

- `convertedProjectId` must be captured BEFORE `_completeClarifyStep` — `resetClarifyState()` is called inside the close flow, which zeroes out `clarifyState` before we can read it
- Auto-flyout is skipped in process sessions (`!!this.processSession`) — opening the project flyout while the next queued task would load creates state conflicts; deferred
- `clarifyDescSummary` listener (~line 431) already does direct mutation of `task.description` in memory; `finalizeClarifyRouting` already reads `task.description` when building the update payload — the save path works, only visibility was missing

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/02-inbox-clarify/02-01-PLAN.md` | The plan to execute |
| `.paul/phases/02-inbox-clarify/CONTEXT.md` | Discussion notes for this phase |
| `app/web_ui/js/ui.js` | Only file being modified (all 3 tasks) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress` to orient
3. Run `/paul:apply .paul/phases/02-inbox-clarify/02-01-PLAN.md` to execute

---

*Handoff created: 2026-05-06*
