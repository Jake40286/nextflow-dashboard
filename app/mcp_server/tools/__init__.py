"""nextflow MCP tool modules.

Empty by design — tool modules are imported by ``server.main()`` so
that the @mcp.tool() registration side effect runs at launch. Importing
this package by itself does not register any tools.
"""
